import { SearchAccountsPipe } from './search-accounts.pipe';

describe('SearchAccountsPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchAccountsPipe();
    expect(pipe).toBeTruthy();
  });
});
